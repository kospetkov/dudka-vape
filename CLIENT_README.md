# 🚀 DudkaVape - Інтернет-магазин вейп продукції

## 📦 Структура проекту

```
vape-shop/
├── client/          # React фронтенд (повна версія)
├── server/          # Node.js + Express бекенд
├── landing/         # HTML лендінг (швидка демо версія)
└── docker-compose.yml
```

---

## ⚡ Швидкий старт (5 хвилин)

### 1. Клонувати репозиторій
```bash
git clone <repo-url>
cd vape-shop
```

### 2. Налаштувати змінні оточення
```bash
cp server/.env.example server/.env
```

Відредагувати `server/.env`:
```env
# MongoDB (обов'язково)
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/vape-shop

# JWT (змініть на свій секрет!)
JWT_SECRET=your_secret_key_here

# Опціонально - для продакшену
LIQPAY_PUBLIC_KEY=your_key
LIQPAY_PRIVATE_KEY=your_key
NOVAPOSHTA_API_KEY=your_key
```

### 3. Запустити
```bash
# Встановити залежності
cd server && npm install
cd ../client && npm install

# Запустити бекенд
cd ../server && npm run dev

# В іншому терміналі - фронтенд
cd client && npm run dev
```

Відкрити: http://localhost:3000

---

## 💳 Налаштування платіжної системи LiqPay

### Крок 1: Реєстрація
1. Перейти на https://www.liqpay.ua/
2. Зареєструватися як бізнес
3. Підтвердити компанію (ФОП або ТОВ)

### Крок 2: Отримати ключі
1. Увійти в https://www.liqpay.ua/admin/business
2. Перейти в "API" розділ
3. Скопіювати `public_key` та `private_key`

### Крок 3: Додати в .env
```env
LIQPAY_PUBLIC_KEY=sandbox_i00000000000
LIQPAY_PRIVATE_KEY=sandbox_xxxxxxxxxxxxxxxx
```

### Тестовий режим (Sandbox)
Для тестування використовуйте тестові ключі (починаються з `sandbox_`).
Тестова картка: `4242 4242 4242 4242`, будь-яка дата, CVV: 123

---

## 📦 Налаштування доставки Нова Пошта

### Крок 1: Отримати API ключ
1. Увійти в https://new.novaposhta.ua/
2. Перейти: Налаштування → API
3. Згенерувати новий ключ

### Крок 2: Додати в .env
```env
NOVAPOSHTA_API_KEY=your_api_key_here
```

### Можливості API:
- ✅ Пошук міст
- ✅ Список відділень/поштоматів
- ✅ Розрахунок вартості доставки
- ✅ Відстеження посилок (ТТН)

---

## 🖼️ Завантаження зображень

### Варіант A: Локальне зберігання (за замовчуванням)
Зображення зберігаються в папці `server/uploads/`
Не потребує додаткових налаштувань.

### Варіант B: Cloudinary (хмарне зберігання)
1. Зареєструватися на https://cloudinary.com/
2. Отримати ключі в Dashboard
3. Додати в .env:
```env
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret
```

---

## 👤 Адмін панель

### Вхід
- URL: http://localhost:3000/admin
- Email: admin@dudkavape.com
- Пароль: admin123 (змініть після першого входу!)

### Можливості:
- 📦 Управління товарами (CRUD)
- 📁 Категорії
- 🛒 Замовлення
- 👥 Користувачі
- 🎨 Налаштування теми
- 🖼️ Слайдер на головній

---

## 🌐 Деплой на продакшен

### Варіант 1: VPS (рекомендовано)
```bash
# На сервері
git clone <repo>
cd vape-shop
docker-compose up -d
```

### Варіант 2: Роздільний деплой
- **Frontend**: Vercel, Netlify, або будь-який статик хостинг
- **Backend**: Railway, Render, або VPS
- **Database**: MongoDB Atlas (безкоштовно до 512MB)

### Налаштування домену
1. Додати A-запис на IP сервера
2. Налаштувати SSL (Let's Encrypt)
3. Оновити `CLIENT_URL` та `SERVER_URL` в .env

---

## 📱 HTML Лендінг (демо версія)

Для швидкої демонстрації клієнту:

1. Відкрити папку `landing/`
2. Файл `index.html` - повністю автономний
3. Можна відкрити локально або залити на будь-який хостинг
4. Працює без бекенду (localStorage)

---

## 🔧 API Endpoints

### Товари
```
GET    /api/products         - Список товарів
GET    /api/products/:id     - Один товар
POST   /api/products         - Створити (admin)
PUT    /api/products/:id     - Оновити (admin)
DELETE /api/products/:id     - Видалити (admin)
```

### Замовлення
```
POST   /api/orders           - Створити замовлення
GET    /api/orders           - Мої замовлення
GET    /api/orders/:id       - Деталі замовлення
```

### Оплата
```
POST   /api/payment/create   - Створити платіж LiqPay
POST   /api/payment/callback - Webhook від LiqPay
```

### Доставка
```
GET    /api/delivery/cities           - Міста НП
GET    /api/delivery/warehouses       - Відділення
POST   /api/delivery/calculate        - Розрахунок вартості
GET    /api/delivery/track/:number    - Відстеження
```

---

## ❓ FAQ

**Q: Чому не працює оплата?**
A: Перевірте що ключі LiqPay правильні та не sandbox для продакшену.

**Q: Не завантажуються відділення НП?**
A: Перевірте NOVAPOSHTA_API_KEY в .env файлі.

**Q: Як змінити тему/кольори?**
A: Адмін панель → Налаштування → Theme Editor

**Q: Як додати нову мову?**
A: Файли перекладів в `client/src/i18n/`

---

## 📞 Підтримка

При виникненні питань:
- Email: support@dudkavape.com
- Telegram: @dudkavape_support

---

## 📄 Ліцензія

MIT License - використовуйте як хочете 🎉
